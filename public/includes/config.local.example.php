<?php
declare(strict_types=1);

/**
 * Bar Tadeo private database configuration template.
 *
 * Copy this file to config.local.php and replace every placeholder.
 * NEVER commit config.local.php or real secrets to GitHub.
 *
 * Gmail/recovery secrets are created separately as recovery.local.php
 * by /tadeo-admin/recovery-setup.php during the one-time live setup.
 */

const DB_HOST = 'YOUR_DB_HOST';
const DB_PORT = 3306;
const DB_NAME = 'YOUR_DB_NAME';
const DB_USER = 'YOUR_DB_USER';
const DB_PASS = 'YOUR_DB_PASSWORD';
